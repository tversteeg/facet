[toolchain]
# Use a single nightly version for everything
channel = "nightly-2025-03-26"
# Specify all components needed for CI and development
components = ["miri", "rust-src", "rustfmt", "clippy"]
# profile = "default" # Optional: keep if you want stable available locally via `rustup default stable`
