{{ header("facet-trait") }}

Defines the main `Facet` trait and implements it for foreign types (mostly `libstd`).

This crate is a core component of facet that enables runtime reflection by providing the foundational trait definition, and implementations for common standard library types.

{{ footer() }}